import { NativeDatetimeTransformerDirective } from './native-datetime-transformer.directive';

describe('NativeDatetimeTransformerDirective', () => {
	it('should create an instance', () => {
		const directive = new NativeDatetimeTransformerDirective();
		expect(directive).toBeTruthy();
	});
});
